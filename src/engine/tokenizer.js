/**
 * TOKENIZER
 * ---------
 * Responsibilities:
 * - Convert expression string into tokens array
 * - Handle:
 *   - Multi-digit numbers
 *   - Decimal numbers
 *   - Unary minus
 *   - Brackets
 * - Throw error for invalid token structure
 */

export function tokenize(expression) {
    // TODO: Implement tokenization logic

    return [];
}