/**
 * OPERATORS CONFIGURATION
 * -----------------------
 * Responsibilities:
 * - Define operator precedence
 * - Define associativity rules
 * - Provide utility helpers
 */

// Return precedence value
export function getPrecedence(operator) {
    // TODO: Define precedence rules
}

// Future:
// export function isLeftAssociative(operator) {}
// export function isOperator(char) {}